#pragma once
#include "window.h"
#include "vertexBuffer.h"

namespace NixEngine {
    class Application
    {
    private:

    public:
        Application();
        ~Application();
        void Run();
    };
}
